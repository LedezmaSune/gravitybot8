{
  "name": "wamasivos",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npx pm2 start dist/index.js --name \"BotMaRe-Engine\" --cwd backend && npx pm2 save",
    "stop": "npx pm2 stop BotMaRe-Engine && npx pm2 delete BotMaRe-Engine && npx pm2 save",
    "list": "npx pm2 list",
    "logs": "npx pm2 logs",
    "install-all": "setup.bat",
    "build:exe": "build_exe.bat",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "BotMaRe - Multi-provider AI WhatsApp Automation Platform",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "pm2": "^5.4.3"
  }
}
